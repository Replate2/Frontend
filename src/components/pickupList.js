import React from "react";

// view pickups {props: filtered list of pickups; type of button to display}
//   single pickup ("you know what nevermind" button)

const PickupList = () => {
    return(
        <div>
            List of pickups for another component.
        </div>
    );
}

export default PickupList;